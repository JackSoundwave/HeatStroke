using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DeployPhaseScript : MonoBehaviour
{
    //Currently this script will remain empty until PoC, just to save time on resources, leaving it here so that you guys understand that.
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
